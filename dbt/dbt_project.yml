name: nyc_taxi
version: "1.0.0"
profile: nyc_taxi
config-version: 2

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
models:
  nyc_taxi:
    staging:
      +materialized: view
    marts:
      +materialized: view