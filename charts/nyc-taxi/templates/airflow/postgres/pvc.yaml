apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: nyc-taxi
  name: airflow-postgres-data
  labels:
    app: airflow-postgres
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 10Gi
