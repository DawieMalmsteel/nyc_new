apiVersion: v1
kind: Service
metadata:
  name: svc-airflow-scheduler
  labels:
    app: airflow-scheduler
spec:
  clusterIP: None
  ports:
  - port: 8793
    targetPort: 8793
    name: logs
  selector:
    app: airflow-scheduler
