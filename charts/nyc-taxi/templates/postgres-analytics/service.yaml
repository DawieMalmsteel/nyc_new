apiVersion: v1
kind: Service
metadata:
  name: svc-postgres-analytics
  labels:
    app: postgres-analytics
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    name: postgres
  selector:
    app: postgres-analytics
