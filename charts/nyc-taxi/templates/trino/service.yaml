apiVersion: v1
kind: Service
metadata:
  namespace: nyc-taxi
  labels:
    app: svc-trino
  name: svc-trino
spec:
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  selector:
    app: trino
