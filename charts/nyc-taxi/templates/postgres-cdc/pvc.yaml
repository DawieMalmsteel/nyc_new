apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: nyc-taxi
  name: postgres-cdc-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard
