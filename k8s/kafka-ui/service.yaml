metadata:
  namespace: nyc-taxi

apiVersion: v1
kind: Service
metadata:
  labels:
    app: svc-kafka-ui
  name: svc-kafka-ui
spec:
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  selector:
    app: kafka-ui
