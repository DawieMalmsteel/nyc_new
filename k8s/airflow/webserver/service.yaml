apiVersion: v1
kind: Service
metadata:
  name: svc-airflow-webserver
  labels:
    app: airflow-webserver
spec:
  ports:
  - port: 8080
    targetPort: 8080
    name: http
  selector:
    app: airflow-webserver
